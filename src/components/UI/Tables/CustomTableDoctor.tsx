import React from 'react';

interface IProps {
  data: any;
}

const CustomTableDoctor: React.FC<IProps> = ({ data }) => {
  return <div>CustomTableDoctor</div>;
};

export default CustomTableDoctor;
